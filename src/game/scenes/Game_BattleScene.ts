import { Scene } from "phaser"

export default class Game_BattleScene extends Scene {
  constructor(){ super("game_battle") }

  preload() {
    
  }

  create() {
    
  }

  update(time, delta) {
    
  }
}