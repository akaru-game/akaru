import { Scene } from "phaser"

export default class Game_RunnerScene extends Scene {
  constructor(){ super("game_runner") }

  preload() {
    
  }

  create() {
    
  }

  update(time, delta) {
    
  }
}