import { Scene } from "phaser"

export default class Game_Match3Scene extends Scene {
  constructor(){ super("game_match") }

  preload() {
    
  }

  create() {
    
  }

  update(time, delta) {
    
  }
}